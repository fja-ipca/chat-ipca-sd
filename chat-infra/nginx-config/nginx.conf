events {}
http {
    server {
        listen 80;
        server_name localhost;
        location / {
            return 200 'Nginx Online! Pronto para a infraestrutura do Chat.';
            add_header Content-Type text/plain;
        }
    }
}